<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página do Funcionário</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #b71c1c;
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      text-align: center;
    }
    header {
      width: 100%;
      max-width: 600px;
      padding: 20px;
      background-color: white;
      color: #b71c1c;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    h1 {
      font-size: 2em;
      margin-bottom: 10px;
    }
    p {
      margin: 10px 0;
    }
    a {
      text-decoration: none;
      color: white;
    }
    button {
      padding: 10px 20px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      margin: 10px 0;
    }
    button:hover {
      background-color: #0056b3;
    }
    .link-container {
      margin-top: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .link-container a {
      padding: 10px 15px;
      background-color: white;
      color: #b71c1c;
      border: 1px solid white;
      border-radius: 5px;
      margin: 5px 0;
    }
    .link-container a:hover {
      background-color: #f5f5f5;
      color: #b71c1c;
    }
  </style>
</head>
<body>
  <header>
    <form method="POST" action="/index/<%= funcionario.id_funcionario %>">
      <h1>Bem-vindo, <%= funcionario.nome_funcionario %>!</h1>
      <p><strong>Cargo:</strong> <%= funcionario.cargo || "Não informado" %></p>
    </form>
    <div class="link-container">
      <a href="/movimentacao/<%= funcionario.id_funcionario %>">Registrar Movimentações
      </a>
      <a href="/movimentacao/funcionario/<%= funcionario.id_funcionario %>/movimentacoes">Ver Movimentações</a>
      <a href="/">Voltar para a página inicial</a>
    </div>
  </header>
</body>
</html>
